# ===========================================
# PostgreSQL 生产环境配置
# 复制为 .env.prod 并填写敏感信息
# ===========================================

# 管理员凭证（必须修改！）
DB_ADMIN_USER=postgres
DB_ADMIN_PASSWORD=StrongP@ssw0rd_2026!  # ← 生产环境必须使用强密码

# 应用数据库配置
DB_NAME=student_management
DB_USER=student_app
DB_PASSWORD=AppStr0ngP@ss_2026!         # ← 应用专用密码

# 网络配置
DB_HOST=0.0.0.0
DB_PORT=5432

# pgAdmin 配置（可选）
PGADMIN_DEFAULT_EMAIL=867225266@qq.com
PGADMIN_DEFAULT_PASSWORD=AdminP@ss_2026!  # ← 仅用于管理界面

# 备份配置
BACKUP_RETENTION_DAYS=7
BACKUP_DIR=/backup/postgres